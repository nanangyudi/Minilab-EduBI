# packages.yml
# File ini diperlukan agar perintah `dbt deps` tidak error.
# Tambahkan package eksternal di sini jika dibutuhkan di masa depan.
# Contoh:
# packages:
#   - package: dbt-labs/dbt_utils
#     version: [">=1.0.0", "<2.0.0"]

packages: []
